# Card Scanner needs no environment variables.
#
# Everything runs in the browser and the one data source (TCGdex) is a free,
# open API that requires no key. This file exists to stay honest about that:
# if a future provider does need a key, document it here and never commit the
# real value.
#
# PORT=8080   # local dev server only (tools/serve.js)
